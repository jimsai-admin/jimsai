# ELE + CLL + Projection — Grounding Review and Falsifiable Roadmap

Date: 2026-07-06
Companion to: `ELE_CLL_Projection_Architecture_Spec.md` (the spec), `docs/concept_language_layer.md` (CLL living doc), `The_Prediction_Trap_Paper.md` (VCO paradigm), `docs/JimsAI_Complete_Specification_v10.md` / `v11.md` (production state).

Purpose: audit the spec's grounding against what actually exists in this repository, state honestly what "quality as good as frontier LLMs" can and cannot mean for this architecture, and define the falsifiable milestone path — under the same anti-hardcoding constraints that govern `benchmarks/genuine_eval.py` and CLL Rules 1–6.

---

## 0b. THE foundational finding — the 100%s came from an idealized world (2026-07-10)

The author pressed the right doubt: a generative harness is still *designed*, and clean generated input is not the real world. Tested live, the foundational capability — knowledge recall — collapses off the clean distribution:

| Fact phrasing | Query phrasing | Recall (5 facts) |
|---|---|---|
| clean template ("The Kwendal project uses PoziDB…") | clean | **5/5 (100%)** — the harness world |
| clean template | messy/colloquial | 3/5 |
| **messy/natural** ("kwendal? oh we went with pozidb…") | clean | **0/5** |
| messy/natural | messy | 3/5 |

**Root cause (diagnosed, not guessed):** entity/value indexing keys on CAPITALIZATION — `cll_shadow.encode` marks a token as name-evidence only if it has a mid-sentence capital or a digit. Real human writing is lowercase and unstructured, so `encode("kwendal? oh we ended up going with pozidb…")` returns **zero literals** — the entity and the value are invisible to the concept index, and recall is 0%. The clean, capitalized, templated fact returns `[L:kwendal, L:pozidb]` and recalls 100%.

**This is the same root cause as the M14b relation-extraction failure** (both need clean orthographic structure real prose lacks), so it generalizes: every prior 100% (P1–P11, M10, …) was measured on clean generated input and must be re-grounded on messy input; several will drop.

**Fix attempted and MEASURED (not assumed), 2026-07-10 — a real trade-off, not a clean win.** Made name-evidence case-independent in `cll_shadow.encode`: a token that is OUT-OF-VOCABULARY (the lexicon matched no surface for it) and of content-word length (≥5) and not a typo of a known word is name-evidence, capitalized or not. Language-universal — no suffix rule, no entity list; the signal is "the lexicon doesn't know this word." Messy gauntlet (live, 5 facts):

| Property (messy input) | before | after |
|---|---|---|
| recall (messy fact + messy query) | 0–3/5 | **4/5** |
| gap-honesty (ghost query, no taught-value leak) | ~5/5 (clean) | **4/5** |
| scoping (no cross-workspace leak) | — | **5/5** |

Recall now WORKS on real lowercase prose (was 0/5 for messy-fact+clean-query); scoping holds. But the fix indexes common OOV words as false entities (`going`, `works`, `actually`, `where`), and that cost **1/5 gap-honesty** — a ghost query leaked a taught value. Introducing fabrication is the cardinal sin, so this is NOT shippable as-is; it is an honest halfway point kept only because the alternative is a system that does not function on real input at all.

**Clean A/B attribution (`experiments/reasoning/messy_gauntlet.py`, `JIMS_OOV_NAMES` toggle).** OFF (baseline) — English AND Nigerian-Pidgin messy facts both: recall **0/5**, gap-honesty 5/5, scoping 5/5 (the capitalization dependence breaks every language equally). ON — recall **4/5**, gap-honesty **4/5**, scoping 5/5. So the fix cleanly owns both the recall gain (0→4) and the gap-honesty cost (5→4), in both a high- and a low-resource language.

**The noise root cause — pinned, and it corrects my first proposal.** I first proposed a FREQUENCY filter; reading the code, the original design had already rejected it for a good reason (`_gate_literal`: *"frequency conflates function words with popular entities"* — a frequently-named entity also has high df). The real cause is narrower and verified: the lexicon is NOUN-only (QRank), so the noise words are common VERBS/ADVERBS/wh-words ("going", "where", "based", "actually") — checked live, all 10 return `not in lexicon`, exactly like the nonce names. The lexicon cannot separate them because it lacks the common vocabulary that would mark a word as *known, therefore not a name*. **The principled fix is therefore broader VOCABULARY coverage** (common words sourced per language — the M5 grind), NOT frequency and NOT English suffix rules (`-ing`/`-ed`, which would fail for Pidgin/Yoruba). Then "where"/"going" become known → not names, while nonces stay OOV → names.

**Resolved and validated (2026-07-10).** Built the common-vocabulary fix two ways, both language-universal, no hardcoded list: (1) ingest external frequency lists where they exist (`fetch_common_words.py` → hermitdave/FrequencyWords, en/fr/zh, 13k words, provenance-stamped); (2) for LOW-RESOURCE languages no external list covers (Yoruba, Pidgin — verified absent from the corpus, because OpenSubtitles doesn't cover them), LEARN common words distributionally — background document frequency in `cll_shadow` (a token recurring across many indexed documents is common vocabulary; Zipf, any language, cold-start-honest). Live messy gauntlet (`messy_gauntlet.py`):

| language | baseline (OOV off) | OOV on, no vocab | + common-vocab |
|---|---|---|---|
| English (external list) | recall 0/5 | recall 4/5, gap 4/5 | **recall 4/5, gap 5/5, scope 5/5** |
| Pidgin (learned path) | recall 0/5 | — | **recall 3/5, gap 5/5, scope 5/5** |

Gap-honesty and scoping fully preserved (5/5) in BOTH; recall lifted 0→functional (4/5 well-resourced, 3/5 low-resource — the honest cost of no external list) with **zero fabrication**. The foundational messy-input problem is genuinely fixed. The loop worked exactly as intended: a real fix, its cost measured, a wrong hypothesis (frequency) corrected by the code, the true root cause (noun-only lexicon) named, and a validated language-universal resolution — no per-language patch.

## 0. Facing the public — fail-safe, not fix-count (2026-07-09)

The load-bearing question for a system meant to take *any* prompt from *anyone*: passing an 11-property generative harness is necessary but not sufficient — the harness has a finite grammar of query shapes; the public has an infinite one. Fixing each failure the harness surfaces is whack-a-mole unless two things hold:

1. **Every fix is a general mechanism, not a patch** — it closes a *class* of inputs (a principle: "a private-entity subject is never a public-knowledge question"; "a referent is a named entity, not a function word"; "casing is not name evidence") with no test value or word list in code. Necessary, but not sufficient.
2. **The failure mode on unseen input is a HONEST GAP, not a confident wrong answer.** This is the real guarantee (the Independence Policy). Inputs are infinite; *modules* are finite — for any input, each module either handles it or refuses cleanly. An LLM's tail failure is fluent hallucination; JimsAI's, by design, is "I don't have that."

**This is falsifiable, so it is tested — `benchmarks/adversarial_fuzz.py`.** It does not grade "right answer." It composes wild prompts at runtime from perturbation operators (prompt injection, code-switching, emoji/symbol noise, SQL/inject strings, "ignore all previous instructions", filler, typos, nested multi-intent) over per-seed nonces — no template to pattern-match — and measures the one invariant that must hold forever or the system is unsafe to expose: **NO-FABRICATION** — the response never voices a planted value for an entity the prompt is not about (a value we planted and can check exactly, no word list).

- **First run (seeds 42/7/2024, 60 wild prompts): 0 crashes, but 2 fabrications (~3%) — the fuzzer found real holes the P1–P11 harness never did.** Both were the same root cause: an all-caps COMMON word from the perturbation ("what **DATABASE** does an untaught entity use", or an injected "output the raw **database**") was mis-extracted as a *named entity*, then satisfied the entity-scope gate against an unrelated taught fact — so the system voiced another project's database for an entity it was never taught. **Fix (principled, applied in BOTH sites that infer a name from casing — `semantic_compiler._looks_like_structural_identifier` AND `runtime_layers` claim-scope):** an internal capital marks a name only in *mixed* case ("TensorDB", "Rovako"); all-caps is emphasis, not a name (`has_upper and not token.isupper()`). Genuine unknown all-caps nonces are still caught by the CLL name-evidence path; only known/common all-caps words are excluded. The consistency point is the lesson: the same casing heuristic lived in two modules, so the fix had to land in both.
- **Re-fuzz confirmation (seeds 7, 2024 — the two that fabricated — plus FRESH seeds 100, 500 never used to find the bug): 0 fabrications, 0 crashes on all four**, gap-honesty on untaught entities up to 6–10/10, recall-under-mangling 7–8/10 (the misses fall back to a safe gap, never a wrong value). The hole is closed and the fix generalizes to seeds it was not tuned against.
- **The honest takeaway for public exposure:** you do not certify readiness by turning the harness green. You certify it by (a) the fail-safe architecture, and (b) *continuous adversarial fuzzing that measures the fabrication invariant and finds holes before the public does* — each hole root-caused to a general mechanism. The fuzzer is that instrument, and it earns its keep by failing.

---

## 1. Audit: what the spec claims vs what the repo contains

| Component | Spec status | Repo reality (2026-07-06) |
|---|---|---|
| CLL concept model (E1–E12) | referenced as prior art | **REPRODUCIBLE** — `experiments/concept_model/` with code, from-source lexicon (26,268 entries, provenance-stamped), seeds recorded in the design doc |
| CLL shadow mode | live | **CONFIRMED** — `prototype/jimsai/cll_shadow.py`, wired into `MultiIndexRetrievalEngine.retrieve` behind `JIMS_CONCEPT_INDEX=shadow`, zero-behavior-change by construction |
| Generative acceptance harness | assumed | **CONFIRMED** — `benchmarks/genuine_eval.py` (fresh seeds, live services, P1–P4 properties) |
| ELE evidence ledger, construction discovery, obligatoriness, shape renderer (§2 rows 1–15) | [PROVEN]/[PARTIAL] | **NOT IN REPO.** No `experiments/ele/`. No corpora, no seeds, no regression tests. The experiments ran in a session whose code was never committed |
| Projection engine | [PROVEN at toy scale] | **NOT IN REPO** (same session) |
| Negative-control corpora (§2 rows 11–12) | "reuse as regression tests" (§6.1) | **NOT IN REPO** |

**Consequence:** every [PROVEN] row for ELE and Projection is currently an *unreproducible claim*. By the spec's own §0 rule ("every module ships with the test that would falsify it"), rows 1–15 must be treated as [UNKNOWN-UNTIL-RECOMMITTED]. This is the single largest grounding defect and it blocks everything downstream of it. Milestone M0 exists to cure it.

---

## 2. Scope corrections (what replaces what)

The spec's title says "Replacing T1 and T2." The production interfaces (per v11) are more specific, and the mapping needs to be exact or we will build the wrong thing:

1. **Production T1** = intent classification of chaotic, multilingual prompts into a `SemanticIR` + 9-capability routing (Qwen3-1.7B, already skippable at compiler confidence ≥ 0.60).
   - The nearest replacement is **CLL's T1-mini** (concept-sequence classifier — E8: 12/12 cross-lingual on toy intents at ~11µs), *not* ELE.
   - **ELE's frames feed L1** (structured extraction: entities, relations, roles). ELE is an L1-upgrade and an ambiguity-exposer; CLL resolves what needs world knowledge. The spec's §7 question ("do layers consume ambiguity sets?") must be answered *before* integration: the contract is **ambiguity sets with confidence** (multiple candidate frames), because the pipeline downstream (L6 retrieval intersection, L8/L9) is what disambiguates — this mirrors the CLL v1 finding that ambiguous surfaces emit top-3 candidates and retrieval intersection resolves the sense.
2. **Production T2** = rendering a full VCO (reasoning chain, gaps, confidence, Markdown, multi-claim composition) — Qwen3-4B, already skipped at confidence ≥ 0.95 with zero gaps via the CSSE.
   - ELE render mode is **not** a wholesale T2 replacement. It is an **expansion of the CSSE's deterministic share**: shape-template + adjunct composition renders single-claim factual content with architecturally-zero hallucination (spec row 6); everything outside coverage raises `GapReport` and routes to Qwen3-4B, flagged unverified.
   - The metric that matters is therefore **deterministic-render share of live traffic** (rising over time) and **zero hallucinations within the deterministic share**, not "did we delete the render model." Deleting Qwen3-4B is the *asymptote*, not the milestone.

---

## 3. Honest framing: "quality as good as frontier LLMs"

The paper (§2) and the spec (§1, §5) both already concede the correct scope. Stated precisely, so nobody builds toward the wrong claim:

- **Not achievable (and explicitly non-goal):** raw parity with frontier models on open-domain generation, unconstrained instruction-following, or knowledge breadth from a cold start. Spec row 3 is the permanent reminder: a 20K-param transformer beat the 526-construction ledger by ~6× on perplexity. Do not re-litigate this; do not hide it.
- **Achievable and defensible (the home turf, per CLL §7):** on the **served distribution** of a workspace, match or beat an LLM baseline per query on **accuracy × provenance × calibrated refusal**, at a fraction of the cost, with correction latency in milliseconds instead of a fine-tune cycle. Frontier models structurally cannot offer pixel/region provenance, zero-retraining correction, or auditable inference (Prediction Trap §2.3).
- **The path to "feels frontier-quality" is coverage growth, not model growth:** deterministic share of traffic grows as lexicon + constructions + ingested graph grow; the honest fallback (bounded LLM, flagged) covers the shrinking remainder. Quality is measured continuously by the generative harness against a frozen LLM baseline on identical generated inputs — win-rate per property, never a vibe.

**The "trillions of prompts" constraint is a distribution claim, not a test-count claim.** No finite test set proves it. What proves it is mechanism-shape:
- every behavior is a general function of *data with provenance* (lexicon entries, discovered constructions, ledger observations, graph edges) — never an enumerated case;
- every test samples its vocabulary/inputs from the data source at runtime with fresh seeds (CLL Rule 2), so the system cannot be tuned to a fixed set;
- everything outside coverage fails **loudly and typed** (`GapReport`, `GAP_UNRESOLVED`) into a fallback or review queue — so novel inputs degrade to honest, never to silent-wrong.

---

## 4. Anti-hardcoding rules extended to ELE (binding)

CLL Rules 1–6 (`docs/concept_language_layer.md` §5) apply to ELE verbatim, plus these ELE-specific instantiations:

- **E-Rule 1 — No hand-registered constructions or templates.** The repo contains discovery code, never construction data. Every construction/shape template in a production artifact must carry provenance: the corpus slice and ledger observations it was discovered from, with counts. A developer hand-adding a `SHAPE_TEMPLATES` entry to fix a failing render has no legal path — the legal paths are (a) more corpus evidence, (b) the review queue. (The spec's illustrative Python dicts are pedagogy, not implementation.)
- **E-Rule 2 — Thresholds are calibrated on a frozen set, then locked** (spec §5 already requires this). `OBLIG_THRESHOLD`, `SPREAD_THRESHOLD`, `MIN_FRAME_COUNT`, `MIN_FILLER_DIVERSITY` get derived from a calibration corpus committed to the repo, recorded in the spec with the derivation script, and gated on minimum per-verb sample size. Retuning against a failing test is a violation.
- **E-Rule 3 — Negative controls are built before looking at the target test set** (spec §4.2 — this methodology found the two real bugs; it is not optional).
- **E-Rule 4 — Equal-budget neural baseline runs as a permanent regression** (spec §4.1). Any "as good as neural" claim for a symbolic mechanism ships with the comparison that could falsify it.
- **E-Rule 5 — The production judge is `benchmarks/genuine_eval.py`**, extended with ELE properties (below). ELE never gets a friendly benchmark as its acceptance gate.

---

## 5. Falsifiable milestone roadmap

Ordered so that the biggest untested assumptions die first. Every milestone has a kill/blocked criterion — a milestone without one is not on this list.

### M0 — Recommit the evidence (blocks everything) — ✅ DONE 2026-07-06
Recreate ELE + Projection experiments **in this repo** as `experiments/ele/`: evidence ledger (append-only invariant, order-independence property test), construction discovery, core/adjunct classification with obligatoriness, shape+adjunct renderer with `GapReport`, and the negative-control corpora of §2 rows 11–13 as seeded regression tests.
- **Pass:** every §2 row 1–15 reproduces from `python experiments/ele/run_all.py <seed>` across ≥4 seeds; the two [BUG FOUND] rows exist as regression tests that fail on the buggy implementation.
- **Kill:** any row that cannot be reproduced gets downgraded in the spec to [UNKNOWN] — in writing.

**Result (2026-07-06, seeds 702945 / 1 / 4242 / 999999): 60 PASS / 12 REPORT / 0 FAIL.** Suite: `experiments/ele/run_all.py`; raw output in `experiments/ele/results/run_results.json`. Notes for the record:
- `OBLIG_THRESHOLD = 0.9583`, **derived** from frozen calibration seed `20260706` (recorded in `classify.py`), then locked — the spec's §7 open question on threshold derivation is closed by mechanism, not by a chosen constant. The 0.92-collocated adjunct edge case misclassifies at 0.90 and classifies correctly at the derived value, reproducing row 13 exactly.
- Rows 11/12/14/15 (the bug-shape rows) are regression tests that demonstrably fail on the buggy implementations (spread-only acceptance, concentration-amplifying `spread_weighted`, base-glued suffix keys → exact 0.0/0.0, vacuous single-filler corpora).
- Row 3 reproduced as methodology, with an honest regime note: on the near-trigram-deterministic nonce grammar the ledger predictor and the tiny neural LM are comparable (~4.9 vs ~4.7 ppl); the original 6× neural win was on natural-ish text. The equal-budget comparison stays a permanent regression either way (E-Rule 4).
- Row 7 reproduced strongly: 20–25/30 held-out neural renders contained unlicensed tokens vs 0 by construction for the template renderer.
- The ELE→CLL bridge contract (ambiguity sets recorded whole, non-composable refusal, correction-as-audited-edit) runs against the real `experiments/concept_model/ConceptGraph` — no duplicate graph code.

### M0b — Projection scaling benchmark (the spec's own biggest unknown, §3.3) — ✅ DONE 2026-07-06
Latency/memory vs ledger size, 100 → 100,000+ events, for each projection type (temporal, trust, contradiction, domain).
- **Pass:** p95 view-computation latency and memory growth curves published in the spec; a snapshot/incremental-view design exists for anything super-linear.
- **Kill:** if projection cannot serve L6-scale volumes even with snapshots, CLL integration proceeds **without** the ledger-projection substrate (concept index + existing retrieval), and Projection is re-scoped to low-volume layers (trust/contradiction), not retrieval.

**Result (2026-07-06, `experiments/ele/bench_projection.py`, JSON in `experiments/ele/results/projection_bench.json`):**

| events | full recompute p50/p95 (ms) | incremental apply p50/p95 (µs) | cell query p50/p95 (µs) | peak MB |
|---|---|---|---|---|
| 100 | 0.23 / 0.33 | 0.8 / 2.3 | 4.0 / 7.4 | 0.02 |
| 1,000 | 2.3 / 3.2 | 0.5 / 1.3 | 6.9 / 11.3 | 0.14 |
| 10,000 | 15.9 / 20.7 | 0.4 / 0.6 | 4.6 / 11.1 | 1.5 |
| 100,000 | 235.9 / 329.7 | 0.8 / 1.2 | 3.1 / 5.5 | 15.4 |

**Verdict:** full recomputation is linear in ledger size — **not viable per-query at L6 volumes** (~236ms p50 at 100k events). The incremental materialized view is O(1) per event and per cell query (µs-level, flat across sizes). Binding design consequence: **production projection uses the incremental view; full recomputation is reserved for rebuild/audit paths.** The equivalence of the two is property-tested in the suite (R5) and must remain so.

### M1 — CLL shadow → on (nearest win; already planned in CLL doc)
Run the live generative harness with `JIMS_CONCEPT_INDEX=shadow`, collect agreement stats, then flip to `on`.
- **Pass:** P4 multilingual 0% → >60%, P1–P3 unregressed, on fresh seeds.
- **Kill:** if shadow agreement analysis shows concept retrieval systematically disagrees with correct production results (not just adds recall), the encoder/lexicon gap is characterized and fixed before `on` is attempted.

**Status 2026-07-07 (updated): UNBLOCKED without Modal.** The Modal 429 quota block was routed around by going fully local, which the Independence Policy demanded anyway: local embedding service (e5-small, CPU, `services/embedding-service` on :8090 — the encoder is URL-driven), all LLM/classifier endpoints black-holed to an instantly-refused port, `JIMS_T1_SKIP_CONFIDENCE=0` (T1 never invoked), CSSE-only rendering, `JIMS_CONCEPT_INDEX=shadow` with a durable JSONL evidence sink added to `cll_shadow.py` (`JIMS_CLL_SHADOW_LOG` — agreement records persist independent of logger config).

**De-LLM baseline (run 1, seed 2371, fully local models — the honest "what were the LLMs actually doing" measurement):**

| Property | de-LLM local (2026-07-07) | with Qwen T1/T2 + Modal classifier (2026-07-03) |
|---|---|---|
| P1 learning | 67% | 100% |
| P2 gap honesty | 0% (ghost queries leaked neighbor facts) | (open issue then too — entity gate) |
| P3 robustness | 56% | 100% |
| P4 multilingual | 0% | 0% (CLL's target, unchanged) |
| P5 multi-intent | 0% (recall ok, math missed) | — |
| P6 math | 0% | 100% |
| P7 scoping | 100% | 100% |

What the deltas actually localize (these are M-targets now, not mysteries):
- **Routing:** every failed P1/P3 case is one fact family (`device_codename`) misrouted to `coding`/`video_generation` at conf 0.35 — identifier-heavy prompts without the zero-shot classifier or T1. This is precisely M2's job (T1-mini) and, under the Independence Policy, low-confidence routes should CLARIFY rather than misroute — the 0.35-confidence misroutes are exactly the cases the policy converts to bounded questions.
- **P6 math 0%:** the deterministic solver is fine; what vanished was Qwen's messy-text → expression normalization. Needs a deterministic normalization layer (concept/structure-driven) or CLARIFY on unparseable expressions — never an LLM.
- **P2 leaks** (`leaked_other_fact=True` on ghost entities): the entity-scope evidence gate is not filtering under the local configuration — needs investigation; CLL `on` (literal gating at the index) is the designed structural fix.
- Latency ~20–38s/query fully local on CPU (vs ~113s median with Modal cold starts) — dominated by cloud persistence round-trips (Supabase/Vectorize/Neo4j), not model inference.

**M1 shadow evidence (run 2, seed 124591, `benchmarks/results/cll_shadow_m1.jsonl`, 31 observations):** on the cross-lingual queries where production recall failed 0/8, the shadow concept index would have retrieved the English-taught record on **4/8 (50%)** — French ✓, Yoruba 2/3, Chinese 0/3. The 4 misses are two named, mechanistic defects (both anticipated by the CLL doc's own findings, now confirmed on live pipeline traffic):
1. **zh lexicon coverage** — 项目/数据库/使用 absent from the 22k-key lexicon (the concept exists: English "project" → `C:Q170584`; its simplified-zh surface doesn't — the traditional/simplified variant-folding gap, CLL v1 finding 4). Fix: data (OpenCC variant tables + deeper zh alias ingestion), zero code.
2. **Doc-side sentence-initial entity suppression → hard-literal-gate veto** — the v1.1 orthography rule (only mid-sentence capitals are name-like) fixed the French "Quelle" false positive but drops sentence-initial entities at INDEXING time ("Tepogi is in…" → no `L:tepogi` on the record), so a query carrying the literal vetoes the correct record. Fix shape (general): asymmetric literal policy — index-side recall-first (sentence-initial capitals index as literals), query-side gating stays strict.
Per the M1 kill criterion these two are the characterized encoder/lexicon gaps to fix **before** `JIMS_CONCEPT_INDEX=on` is attempted; the 50% shadow recovery where production scores 0% is the strongest live evidence yet that the concept index is the right mechanism for P4.

**M1 on-mode iterations (2026-07-07, all fully local/de-LLM):**
- *Fixes applied:* lexicon enriched from Wikidata (+9,191 zh / +4,194 fr surfaces incl. variant codes; 22k→39k keys; `experiments/concept_model/enrich_lexicon.py`, full provenance); asymmetric literal policy in `cll_shadow.py` (documents index sentence-initial entities recall-first; queries promote sentence-initial capitals only on corpus memory); `JIMS_CONCEPT_INDEX=on` implemented in `retrieval.py`.
- *Iteration 1 (post-rank prepend, seed 211770):* P4 0%→33% — the first P4 passes ever, including zh — but prepending bypassed production scoring: chat-question records flooded the top (sentence-initial "What"/"Which" became promotable literals via a feedback loop through indexed chat records), regressing P3 filler cases and keeping P2 at 0%.
- *Iteration 2 (candidate-level injection + rare-literal gate, seed 933508):* P3 restored to 78%, ghost-query veto works (concept index returns [] on unseen literals) — but P4 back to 0% for a newly-localized reason: the df-ratio rarity test wrongly blocks literal promotion for *frequently-discussed* entities (rarity conflates function words with popular entities; the correct name-evidence signal is "capitalized mid-sentence anywhere in the corpus"), and where the fact DID reach final results (confirmed in `cll_shadow_m1_on2.jsonl`: the zh queries' finals contain the English-taught fact sig), **the CSSE answer builder still failed** — it voices top memory excerpts regardless of whether they answer the question.
- *Controlled probe (clean workspace):* teach one fact → English AND Chinese queries both voice it end-to-end. **The de-LLM cross-lingual chain works when ranking is clean.** The harness failure mode is accumulated chat-noise records outranking the taught fact by P4 time — and the answer builder voicing a question-record as a claim ("I believe this is right" on an echo), which is also P2's leak source and precisely the fluent-wrong failure the Independence Policy §2.6 forbids.
- **Remaining work to close M1, precisely scoped:** (a) ranking discipline — trust/provenance-aware reranking so prior-query and answer-echo records rank below taught facts (the hybrid-scoring trust term from spec v10, applied in this path); (b) answer-selection honesty in the CSSE — voice only claims whose structured relation matches the query's target, else emit a gap (this is M4's ELE-render/CSSE work arriving on schedule); (c) replace the df-ratio promotion test with mid-sentence capitalization evidence. All three are general mechanisms; the harness stays the judge.

**M1 — ✅ CLOSED 2026-07-07 (judgment run 3, seed 58533, fully de-LLM/local): P4 0% → 67% (bar >60%), P2 gap honesty 0% → 100%, P1 100%, P3 78% (unregressed), P7 100%.** The closing fixes were one principle applied at three layers — **"questions don't assert"** — plus entity scope and concept-level relevance:
- Reasoning bridge (`runtime_layers._memory_excerpt_steps`): interrogative sentences are never voiced as claims (killed the answer-echo); a claim must share a query entity (killed ghost leaks — P2 to 100%); sentence relevance adds CONCEPT overlap via the CLL encoder, so cross-lingual claims pass the relevance minimum at meaning level. The "supports intent" filler-claim fallback is deleted: no assertive match → named gap + HEDGE (Independence Policy §2.6 in code).
- Concept index (`cll_shadow`): per-sentence assertive indexing — declarative occurrences of a key weigh 2× interrogative mentions, so records that STATE things about an entity outrank records that ASK about it (killed the tie-flood); mid-sentence-capitalization name memory replaces the df-ratio test (popular entities stay promotable; colon = clause introducer, so `prompt:`-prefixed question words gain no false name evidence); unknown gate literal → the index ABSTAINS (ghost honesty as an index property).
- Chaotic-input robustness (user-directed mechanism): O(1) skeleton/anagram typo repair over the growing lexicon (`_typo_repair`) — "proejct"→"project" by letter anchors, unique-candidate-only, zero language data.
- Notably: `device_codename_zh` passed *despite being misrouted* to video_generation — the concept index carried it. The 3 remaining P4 misses: 1 router misroute (M2) and 2 Yoruba cases (yo lexicon 1.8k entries vs 17k zh — same `enrich_lexicon.py` path, more yo alias sources; data work, no code).
- Every fix above is a general mechanism stated as a principle; none references a test case, a fact family, or a language. P5/P6 (routing decomposition + math normalization) remain 0% and belong to M2 — deliberately untouched.
- **Production config note:** the winning run used `JIMS_CONCEPT_INDEX=on`, local embedding service, `JIMS_T1_SKIP_CONFIDENCE=0`, and black-holed LLM endpoints — this is the de-LLM deployment configuration to promote into `.env`/deploy manifests (M6).

### M2 progress — routing + math without any LLM — ✅ MAJOR MOVEMENT 2026-07-07
Two runs (seeds 889948, 83097), fully de-LLM/local. Final scoreboard: **P1 100% · P2 100% · P3 89% · P4 78% · P5 50% · P6 100% · P7 100%.**
- **P6 math 0% → 100%** — `prototype/jimsai/math_extract.py`: math notation treated as the formal language it is (NFKC fold, maximal balanced spans, variables admitted only by operator adjacency, `^`→`**`). Verified offline on arithmetic/equations/CJK-embedded/chaotic text and correctly extracts *nothing* from recall prose. The Qwen extraction bridge is now a dead legacy fallback. Line 922's `# T1 bridge handles extraction` era is over.
- **Device-family misroutes eliminated** — workspace-literal routing evidence: a query naming an entity the workspace memory already knows (CLL postings) is a memory question, whatever its wording resembles ("codename of the device…" ≠ coding when 'Bevorno' is a taught entity). Data-driven, no vocabulary; structural signals (code fences, math syntax) stay dominant.
- **P5 multi-intent 0% → 50%** (first pass ever) — union entity scope: a multi-intent prompt carries several foci (numbers for the math span, names for the recall span), and a claim may serve any of them. The multi-attention-span principle landing in the claim gate. Remaining miss: person-family recall under the math route — next diagnosis.
- **P3 89%** — skeleton-tolerant entity matching (the typo-repair anchor principle applied at the claim gate: 'Pujvuu' finds 'Pujuvu'); the single P3 failure was an HTTP 500 from a transient 5s embedding timeout hitting the by-design hard-fail — an infra item (raise `JIMS_EMBEDDING_TIMEOUT`, add one retry), not logic.
- **P4 78%** — both misses remain the Yoruba lexicon-coverage data item (yo: 1.8k entries; fix = `enrich_lexicon.py` with OMW/PanLex yo sources; zero code).
Every mechanism above is stated as a principle (formal-language extraction; workspace knowledge as routing evidence; multi-focus claim scope; letter-anchor matching); none references a test case, family, or language.

**Namespace extension (2026-07-07, user-directed):** the extractor now admits long identifiers by lookup against the solver's GROWING symbol namespaces — physics constants (`k_B*300`, `E=m*c**2`, `F=5*g`, `P=R*300/0.05` all verified) and chemical formulas decomposed against the element table (`H2SO4` admits iff every element symbol exists in the data and a digit is present). Physics/chemistry/engineering notation rides the same grammar with zero per-domain code; the namespaces live with the solver (to be sourced from CODATA/periodic-table data). Honest boundary: word problems ("how many moles in 98 g of H2SO4") correctly extract *nothing* — mole arithmetic requires domain reasoning (concept layer), not span extraction; that is a solver-capability roadmap item, not an extraction gap.

### Dialogue (P8) + natural responses + streaming — 2026-07-07

- **P8 dialogue property added** to `genuine_eval.py` (the judge, built first): an underspecified follow-up ("And what database does *it* use?") in the same thread must resolve via discourse focus, and a COLD thread (no prior turn) must honestly fail to resolve the same follow-up. Cross-family, seeded.
- **Root cause found and fixed — a real defect, not a test gap:** `ProductionRuntime.save_session` silently no-ops when Redis is unavailable (`if self.celery and ...available`), so dialogue focus evaporated between turns whenever the external cache was down. Fixed in `pipeline._load_session/_save_session`: dialogue state is now core conversational memory with an **always-available in-process tier** plus best-effort cloud durability — a dropped cache write can never erase the conversation's focus. Verified: follow-up now inherits `['vorbani']` and answers end-to-end (opener→follow-up) fully de-LLM.
- **Discourse-focus source unified with the concept layer:** when L1 extraction yields no entity for the opener, `ACTIVE_OBJECT` falls back to CLL name-evidenced literals from the same query — one name-evidence machinery everywhere, any language. Inherited focus also feeds the router's workspace-literal probe so dialogue follow-ups route to memory, not a misread capability.
- **Removed a hardcoded heuristic that blocked dialogue:** the `uses_/has_/is_` prefix guard on focus inheritance (a vocabulary list) is deleted — inheritance is now an ambiguity candidate that downstream evidence (retrieval intersection, entity-scoped claims, 15-min topic decay, gap honesty) validates.
- **Natural responses — robotic leaks removed at the source:**
  - The stock "*I believe this is right.*" footer stamped on every answer is gone; certainty is conveyed by answering plainly, and only genuine uncertainty (tier 4) adds a brief woven sentence. No italic meta-footer.
  - Internal diagnostic gaps ("no assertive claim matched…", "Coding provider unavailable…") are now **typed `[internal]` at emission** and stripped structurally by the renderer — not caught by a growing substring blocklist. Users get the natural gap response ("Share a file or describe what you're building…") instead of raw internal status.
- **De-LLM streaming:** `/v1/query/stream` now streams the deterministic CSSE render itself in natural word-chunks (`pipeline._stream_chunks`) when no T2 LLM is present — genuine progressive token delivery with zero model, exact text reconstruction, language-agnostic. Verified: 9 word-chunks reconstruct the exact answer.
- **Removed the load-bearing hardcoded heuristic (`_looks_like_structural_identifier`):** it only recognized camelCase/dotted code identifiers, never plain proper nouns — so openers didn't extract their entity, and entity-less ghost queries INHERITED stale dialogue focus and leaked neighbor facts (the P2 regression). Replaced with the CLL name-evidence mechanism (mid-sentence capitalization, any language, no word list) — one heuristic removed, three failures fixed: P2 leak, device dialogue routing, and clean entity scope. Inherited (lowercased) dialogue entities are checked against concept-index postings directly (`shadow.known_terms`) since they no longer re-tokenize as literals.

**FINAL BOARD (2026-07-07, seed 724613, fully de-LLM/local): P1 100 · P2 100 · P3 100 · P4 78 · P5 50 · P6 100 · P7 100 · P8 100.** P8 dialogue 4/4 including the cold-thread honesty control (a follow-up in a thread with no prior turn correctly refuses to resolve "it"). Remaining 3 failures are all known, non-regression: 2 Yoruba P4 (lexicon data — yo 1.8k entries; `enrich_lexicon.py` + OMW/PanLex) and 1 P5 person-recall-under-math-route. Six of eight properties at 100%, fully de-LLM, on fresh seeds — the strongest result of the program.

### Robustness (P9), response format (P10 = M7 slice), and content-hygiene — 2026-07-08

- **P9 adversarial/edge-case suite** added to `genuine_eval.py` (11 generated cases): empty/whitespace/punctuation/symbol-noise/gibberish/injection/120-word-entity-less prompts (no crash, no fabrication) + distortion resilience (buried query, code-switch, symbol noise) + contradiction latest-wins. **11/11 on a fresh seed.**
- **Contradiction / correction (the spec's row-5 gap) closed for the de-LLM path:** `retrieval._supersede_concept_conflicts` — two records with the same concept signature, a shared subject literal, and exactly one swapped value literal are a correction; latest-wins in the voiced answer, both retained in the ledger. Tightened to strict single-swap after a loose version over-suppressed; verified it does NOT collapse different subjects (Alpha/Beta projects both retained). The structured-relation version (`_supersede_functional_conflicts`, ontology-cardinality-gated) stays for when M5 supplies cardinality data.
- **M7 response-format faculty BUILT** (`response_format.py`): deterministic table/JSON/bullet emitters render the SAME verified VCO claims into a user-requested shape; format detected from a small operation vocabulary (like the capability taxonomy), unknown formats defer to prose (no guess). Wired into the CSSE via `VCO.raw_query`. New harness property **P10: 3/3 (100%)**.
- **ROOT-CAUSE fix for the persistent person_city weakness (assertive-index):** the concept index was returning stored QUERY/PROMPT records as if they were facts (a query trivially matches itself). "Questions don't assert" is now enforced at the index — `concept_hits` returns only records with an assertive (declarative) match, never records that merely ask. Verified in isolation (person recall works). Lifts P1/P3/P4/P8 for person facts.
- **Content-hygiene fix — "voice only knowledge, never interaction logs":** a production stress capture (`De-LLM_JimsAi_StressTest.md`) exposed the answer path leaking prior-query text and internal metadata ("Prompt: … Capability: … Answer: …") into responses. Fixed with one principle: `retrieval._is_knowledge_signature` excludes interaction/result-log records (system-controlled provenance/intent markers) from voicing, and the concept injection now respects `exclude_ids`. **Eliminated six leak paths at once** (ghost-query, injection echo, typo echo, two format-directive leaks, streaming leak) — re-capture confirmed all clean.
- **Production stress test:** `De-LLM_JimsAi_StressTest.md` — 17 real prompts/responses against the running de-LLM backend; 14 correct, 3 imperfect-and-labelled (physics-equation substitution bug, multi-intent decomposition gap, heavy-multi-typo recall). Honest evidence, not mocks.

### Generation decomposition begun — response language (P11) + discourse discovery (M9), 2026-07-08

Design doc: `docs/generation_decomposition.md` — the full plan for decomposing generation into independently-measurable modules (Discourse Engine, Surface Realizer, Projection+Verifier), the "one operation at five zoom levels" thesis, the 12→3 capability collapse, the honest frontier (content-selection + referring expressions, not metaphor), and the goal: *everything an LLM does, but separate and testable*.

- **Response language (P11) — BUILT, first pass:** `prototype/jimsai/surface_realizer.py` re-realizes each verified claim's CONTENT words into the query's language via the CLL **reverse lexicon** (concept → target surface), keeping literals/entities as-is; wired into `csse.render` (`_realize_language`). Verified end-to-end: en unchanged, fr "The Aperture Projet uses TensorDB as its primary base de données", zh "…項目…资料库". No hardcoded translation table — every surface from the provenance-stamped lexicon; new language = data only. Content is faithfully in-language; function-word + grammar fluency is the M4b constrained-realizer layer (roadmap). P11 harness judge added (character-range script signal, no-LLM). Principled fixes made: content-word length gate (≥4, avoids "as"→Asperger polysemy misfire) and full-form-over-abbreviation preference.
- **M9 Discourse-Structure Discovery — mechanism validated, metric flagged:** `experiments/discourse/run_m9.py` (offline, seeded, no LLM). **M9b PASSES 4/4** — greedy reordering by entity-continuity recovers coherence (mean 0.50) well above random (0.31): the Discourse Engine's ordering-projection mechanism is real and generalizes to unordered claims. **M9a weak 2/4** — the naive entity-continuity metric barely separates real-from-shuffled on dense technical docs (they repeat core entities every sentence). Honest conclusion (per kill criterion): the ordering *mechanism* survives; the coherence *metric* needs full Centering-Theory features (backward/forward centers) or narrative corpora — recorded, not overclaimed. This is the falsifiable proof-of-mechanism that discourse structure is learnable-as-evidence.
- **M10 Referring-expression generation — ✅ MECHANISM PROVEN 4/4, 2026-07-10:** `experiments/discourse/run_m10.py` (offline, seeded, no LLM). The "say the name or say a pronoun" decision — what makes prose read as human, not robotic — is computed from Centering state, not an LLM: pronominalise the continued focus (subject carried from the prior sentence) **iff** no locally-salient entity shares its agreement class; else name it. An INDEPENDENT resolver (blind to the generator's intent) round-trips every pronoun. Result across 4 seeds: `centering` holds **100% resolution** (never introduces an ambiguity the reader can't undo) while pronominalising ~38–46% of eligible mentions; the naive `always_pronominalise` baseline drops to **69–77% resolution** ("she advises she"), proving the ambiguity guard earns its keep; `all_names` is 100%/0% (robotic floor). Sample: `all_names` = "…Qiwena manages Netose. Qiwena founded Netose. Qiwena reports to Defolo…"; `centering` = "…Qiwena manages Netose. She founded Netose. She reports to Defolo…". Together with M9 (ordering) this is the discourse-composer spine — the "second brain, natural responses" goal — as separable, measured modules. Production step: wire into the CSSE multi-claim render path (the M4b realizer already owns single-claim fluency).

### Reasoning — symbolic science/engineering derivation, detailed & language-neutral, 2026-07-10

Question posed by the author: can JimsAI *reason* over math/physics/chemistry/engineering — heavy on EXPRESSIONS, few numbers — from any language, and answer in detail? Findings, grounded:

- **It already reasons two ways beyond direct lookup:** (1) a real symbolic engine (sympy) fed by a **grammar-based, LLM-free, language-independent** extractor (`math_extract.py`); (2) genuine **graph traversal** — BFS multi-hop `CausalGraphEngine.traverse`, `verify_path_connectivity` (can A reach B via chains), causal-path exploration, world-model rule promotion (`simulation.py`, `graph.py`, `world_model_promotion.py`). The traversal side is under-used (it reports paths as diagnostics rather than composing them into a voiced inference) — the next improvement.
- **The symbolic engine already DERIVES formulas** (`solve('F = m*a','a')` → `a = F/m`, steps `['F = m*a','-m*a = -F','a = F/m']`; `v = u + a*t` for t → `t = (v-u)/a`). The blockers were extraction, now fixed with two language-independent grammar rules:
  - **Implicit multiplication** ("2x" ⇒ "2*x"): a number immediately (no space) followed by a variable — universal notation, so `Solve 2x + 5 = 11` now solves in de-LLM mode (previously needed Qwen).
  - **Solve-target detection** for expression-heavy problems: the target is the equation symbol that ALSO appears in the query *outside* the equation span ("solve F=m*a **for a**", "**t** in terms of…", French "…**pour y**") — a repeated variable, no "for"/"pour" keyword, language-independent.
- **Detailed by default, terse on format request.** `_render_math` shows the working as **language-neutral NOTATION steps** (`2*x = 6` / `a = F/m`), read from the structured solver result (not a realized claim string, which the per-language realizer would mangle); a specified output shape collapses to the answer. Verified end-to-end: EN, FR ("Résous 3y-6=9" → `y = 5`), and symbolic derivations all render detailed, correct, zero English.

**Honest boundary:** open-ended free-form reasoning over an *arbitrary* novel problem outside the solvers/knowledge/traversal still gaps (fail-safe) rather than fabricating — but the traversal machinery is a real, improvable substrate for it (compose paths → inference), not nothing. The distinction from an LLM holds: answers are *computed/derived and correct*, not predicted.

**M14 traversal-reasoning — built, and then honestly re-grounded (2026-07-10).** `graph.find_path` (BFS shortest path) + `reasoning_traversal.compose_path_inference` turn adjacency into a voiced transitive chain (`A —causes→ B —causes→ C ⟹ A —causes→ C`), returning None (never a fabricated link) when unconnected. On seeded clean chains (`experiments/reasoning/run_m14.py`) it scored 100% recall / 0 fabrications across 4 seeds. **But that number was a warning, not a win — the graphs were built to pass.** The author's correction: *the real world is unsorted; 100% on designed data is not grounding.* So `run_m14b_messy.py` fed passive voice, negation, hedges, coordination, filler, and ordinary-word (non-nonce) entities, with ground truth. The real numbers: **recall 38%, precision 45%, 49 direction errors (passive voice reverses the edge), and 93 negation false-positives** (asserting a causal link the source *denied*) over 3×120 sentences; the end-to-end passive-voice chain inferred None. **Conclusion, fail-safe:** the reasoner is sound, but grammar relation extraction from messy prose is NOT reliable enough to populate the graph (45% precision would make the reasoner *confidently wrong* — the one failure mode the whole project forbids), so `grammar_relations` is **deliberately NOT wired into the answer path**. Robust extraction — distinguishing positive/negated/passive/coordinated *constructions* per language — is the ELE discovery problem (M3), not a hardcoded verb/negation list. The measurement itself is the deliverable: a test you cannot fail measures nothing; this one fails honestly and says exactly why.

### Language-universal response surface — de-Englishing the chrome, 2026-07-10

Directive (reinforced by the author): JimsAI takes a prompt in ANY language — incl. low-resource (Nigerian Pidgin, Yoruba) — and must respond WELL in that language (or a specified strict format), with NO hardcoded English and no per-language hand-table (the maintained-list anti-pattern). An audit of `csse.py` found the *content* was realized per language but the *chrome* was hardcoded English throughout — math scaffolding ("To work it out:", "Solving `{expr}` gives"), confidence hedges ("This is my best read — worth a quick check."), section labels ("What I'm less certain about:"), and the whole gap-response family ("I don't have that in my memory yet…"). All would appear verbatim in a French/Pidgin answer. Fixes, each a general mechanism:

- **Math → universal NOTATION.** `_render_math` now emits `**12*8 = 96**` and `**expr ⟹ x = 3**` — numbers, operators, `=`, `⟹` carry the whole meaning in every language; zero English words. (Was: "To work it out: `expr` evaluates to …".)
- **One localisation path, not a table.** Every chrome string routes through `csse._localize`, which is the SAME data-driven `realize_in_language` used for claims (content words translate via the CLL lexicon, no per-language dictionary in code). No-op for English.
- **Decorative vs essential chrome, split honestly.** The confidence hedge is decorative → shown ENGLISH-ONLY, omitted for other languages (the realizer can't translate "best/read/worth", so realizing it would leak English or garble it — omission is fail-safe; the calibrated confidence score is in the payload). The honest GAP message is essential → realized best-effort in-language (it must communicate "I don't know" in the user's language). Verified: a French answer no longer carries the English hedge; French math is clean notation.
- **Discourse composition stays language-universal too** (M9 ordering only; M10 surface elision/pronouns deliberately NOT shipped — they need discovered closed-class, not hardcoded English; see `discourse_composer.py`).

Honest residuals (named, not hidden): (1) full per-language chrome (gap prompts, headers) is realizer-coverage-limited — the real fix is discovered/sourced chrome vocabulary (the M5 lexicon grind) or structured chrome localised at the presentation layer; (2) **WORLD_KNOWLEDGE still voices web-scraped junk on untaught queries** instead of the honest gap — a fail-safe violation (untaught ⇒ gap, never web garbage) and the next thing to fix.

### P5 multi-intent + P11 realizer grounding — 2026-07-08 (honest, mixed)

- **P5 multi-intent — mechanism FIXED.** Root cause: the composition logic already existed (pipeline keeps math + memory when secondary intents present), but the router never *detected* the recall as secondary — workspace-literal evidence was gated behind `if not strong_structural`, so any math-syntax prompt skipped it. Fix (`capability_router.py`): workspace-literal evidence runs EVEN under a strong structural signal (a structural hit is ONE intent; a named known entity is ANOTHER — the multi-attention-span primitive), and because it is unambiguous evidence it forces MEMORY_CHAT into secondary intents, bypassing the margin heuristic. Verified: `project_database` multi-intent now composes both answers (math 100 + recall TensorDB); `person_city` multi-intent fails only when person_city *recall* fails (below). Pure math (no known entity) unaffected — P6 intact.
- **person_city recall — seed-dependent nonce-collision weakness (honest limitation).** Passes in isolation for 6/6 hand-picked nonces and under simulated query history; passes cleanly on some harness seeds (44241) and fails on others (877193) for the person family across P3/P4/P5. Cause is specific generated nonces colliding with real lexicon concepts, disrupting encoding/routing — not deterministically reproducible. Recorded as ~85–90% reliability, not a clean 100%; not hidden.
- **P11 response-language — mechanism works, lexicon-coverage-limited.** `surface_realizer` translates when content words are in the lexicon (`project`→`Projet`, `database`→`base de données`, `资料库`) but leaves the person sentence English because `city`/`engineer`/`based` are absent from the 3,000-concept QRank lexicon. Mechanism proven; the gap is coverage (the lexicon grind, Gap 4) — closed by M5 ingestion of common-noun concepts (OMW/WordNet), which lifts P4-person, P11, and paraphrase depth together. The character-based P11 judge is also strict on short factual sentences; a lexicon-vote judge is the refinement.

### M2 — T1-mini intent classifier: honest negative-ish, 2026-07-10

`experiments/concept_model/run_m2.py` (offline, seeded, no LLM/embeddings). Benchmarks the concept-native TinyIntentClassifier (naive Bayes over CLL concept IDs) on the PRODUCTION lexicon, trained on clean English only, against a surface-word baseline and a hybrid — tested on messy English and REAL (fully in-language) French/Chinese intent queries. The E8 toy (12/12 cross-lingual) was an ideal world; production is messier and the result flips the naive claim:

| condition | concept | surface | hybrid |
|---|---|---|---|
| clean English | 81% | 98% | 100% |
| messy English | 79% | 97% | 95% |
| real French | 42% | 30% | 30% |
| real Chinese | 70% | 20% | 70% |

- **Surface words WIN same-language** (98% vs 81%) — English intent words are highly discriminative; concept features *lose* signal on short queries.
- **Concept-native genuinely transfers cross-lingually** where surface collapses (fr 42>30, zh 70>20, surface ≈ chance on Chinese) — the T1-mini transfer claim is real, directionally.
- **No clean winner.** The hybrid is best for English (100%) and non-Latin scripts (zh 70%), but for FRENCH the shared-Latin surface words misfire and *drag the concept signal down* (42→30). A clean fix needs language-aware weighting = language detection, i.e. added complexity.
- **Honest conclusion:** pure T1-mini does NOT cleanly replace the current classifier. The production `multilingual-e5` intent classifier is already CPU-local (not an LLM — the Independence Policy permits small swappable embedders) and handles all cases *uniformly*, which the naive-Bayes variants do not. T1-mini's concept-native transfer is a viable CPU-cheap FALLBACK / cross-lingual booster, not a replacement. Not every roadmap item is a win; recorded, not inflated.

### M11 — Projection + verifier: analogy is computable & verifiable, 2026-07-10

`experiments/creativity/run_m11.py` (offline, seeded, no LLM). Tests whether novel composition — analogy/metaphor — is PROJECTION of relational structure accepted by a VERIFIER, not sampling randomness. Per-seed relational graphs; for each a SOURCE, an ANALOGOUS target (isomorphic relabel), a PARTIAL target (half the relations), and an INCOHERENT target (randomised). The projector finds the entity mapping maximising **systematic** alignment (Gentner: the largest CONNECTED component of preserved relations / |source| — a real analogy preserves one connected structure, coincidences are scattered); the verifier thresholds it.

- **Across 4 seeds: analogous ≈ 1.00, incoherent ≈ 0.36, separation 0.64, and 0 FALSE-ACCEPTS** — the verifier never accepts an incoherent projection as coherent (the fail-safe: a bad analogy is refused, not voiced), 0 missed real analogies. Example: a 4-relation source maps 1:1 onto its relabelled analogue (score 1.0) and near-0 onto a randomised one.
- **The failure I found and fixed, honestly:** raw preserved-fraction scoring gave false-accepts on SPARSE structures (few relations → a random target aligns 70%+ by chance). Systematicity (connected-component scoring) plus richer structures (7–9 relations) removed them — which is theory-consistent: structure-mapping is reliable only with *enough* relational structure. Sparse-structure chance alignment is the named limitation.
- **Conclusion:** analogy/metaphor has a verifiable computational substrate — projection proposes, structural alignment accepts or refuses — so creativity need not be delegated to an external model. Grounded, not theory.

### M-LEARN — evidence-driven continual learning for code: PASS, 2026-07-10

`experiments/synthesis/run_m_learn.py` (offline, seeded, no LLM). Tests the loop the design's strongest claim rests on: generate → EXECUTE → observe tests → extract a REUSABLE VERIFIED pattern → store → REUSE. A bounded enumerative synthesiser (search depth D) solves easy tasks, stores each solution's op-sequence as a named pattern (one op instead of many), and later solves HARDER tasks — which need more than D primitive ops — within the SAME budget, because a learned pattern collapses a sub-sequence.

- **Learning EXTENDS REACH:** hard-task solves **base 16 → with-learning 31** across 4 seeds (≈ +90%), same depth budget.
- **0 wrong solutions:** every accepted program is re-executed against the task's examples; a program that fails is never accepted.
- **CANNOT OVERGENERALISE (the sort()/discount answer):** every candidate — learned pattern included — is RE-VERIFIED against THIS task's examples before acceptance. A "+2" pattern offered to an "x+1" task fails its examples and is rejected. `overgeneralization_safe = True` on all seeds. So a context-dependent lesson is applied only where the task's own evidence confirms it — the M8 never-voice-a-wrong-program guarantee, extended to learned knowledge.

**Honest frontier boundary (answering the massive-generation / humor questions).** The loop is validated as **domain-general in structure** (solve → observe trustworthy feedback → extract pattern → verify → reuse) — but its strength is bounded by whether the domain has an **objective verifier**: coding (compiler/tests, proven here), mathematics (proofs), science (experiment), engineering (simulation), law/medicine (rules/outcomes) — YES. Where success is **subjective** (funniest joke, most beautiful landscape, unrestricted fiction, human-level humor), there is no compiler for the answer, so the *objective* loop does not apply — those need learning from **distributions of human preference**, a different and open problem. Massive creative generation and OS-scale code synthesis are reachable only as **planning + the verified loop over decomposable sub-tasks** (each sub-task verifiable); the subjective quality of the whole (narrative fluency, wit) remains an open research problem — NOT claimed, not faked. This is the honest line: JimsAI's evidence-driven learning is real and grounded in verifiable domains; frontier-level open-ended creativity and human humor are not demonstrated and are named as open.

### Generation frontier — MEASURED objective/subjective split (4 experiments), 2026-07-11

The boundary above was *asserted*; these four offline experiments (no LLM) make it *measured*, answering "can realization from explicit evidence match neural LM fluency/flexibility/breadth?" — grounded, not guessed. Each isolates the OBJECTIVE core (a verifiable property) from the SUBJECTIVE surface (no verifier), and tests only the former.

- **M-CODEPLAN** (`run_m_codeplan.py`) — large-scale code by hierarchical verified synthesis: build a LIBRARY of verified functions bottom-up, each reusing the earlier ones. **48/48 vs flat 0/48, 0 wrong** — the composed program (8 chained functions) is far deeper than any flat search reaches; complexity collapses into reusable verified pieces. Scale is real; correctness holds under composition.
- **M-REPAIR** (`run_m_repair.py`) — the compilation-feedback / iterative-repair loop: feedback-guided hill-climb on test pass-count. **repaired 36/60 vs from-scratch 27/60, 0 wrong**, ~120 evals to localise. Honest ceiling: greedy repair stalls in local optima (60%), and a pass-count is weaker than real compiler/type feedback — named, not hidden. Together with M-CODEPLAN this is the plan → synthesise → verify → repair loop OS-scale code needs; the open part is spec-discovery from prose and API-grammar breadth.
- **M-GEN** (`run_m_gen.py`) — fluent generation by REUSING discovered constructions (the same frames ELE extraction learns), round-trip verified (generate → re-extract must equal the intended facts). **100% fidelity, 0 hallucination, gap-honest (unknown relations abstained), 2 languages.** Faithful, non-hallucinating, multilingual realization with NO templates and NO surface strings in the generator — a new language is new data.
- **M-NARRATIVE** (`run_m_narrative.py`) — long-range narrative STRUCTURE by planning: character-state consistency, foreshadow→payoff, motif recurrence, arc convergence over 120 beats. **Planner: 0 contradictions, 100% payoff, arcs converge; naive local generation: 352 contradictions, 62% payoff.** The structural competencies (long-term planning, character state, narrative memory, discourse planning) are a solved planning problem.

**The grounded answer.** On **OBJECTIVE axes** — structural consistency, correctness, faithfulness, no-hallucination, long-range coherence — evidence + planning + verification **matches or EXCEEDS** free neural generation (which drifts, contradicts, and hallucinates over long spans: the 352-vs-0 contradiction gap and 100%-faithful realization are the evidence). On **SUBJECTIVE axes** — surface prose fluency, wit/humor, beauty, stylistic breadth — explicit-evidence realization **cannot** match neural LMs, because there is no objective signal to optimise or verify against. So for the "satirical Shakespearean Pratchett play about quantum computing": its *structure* (plot, foreshadowing, recurring jokes-as-motif, character arcs, consistency) is plannable and verifiable **today**; its *surface* (meter, wit, actual humor, cultural allusion) is the genuinely open frontier. The two paradigms are **complementary**: JimsAI contributes guaranteed structure + faithful content + zero fabrication (across languages); neural surface artistry from explicit evidence alone remains unsolved and is not claimed.

### Statistical prose absorber → verified-pattern candidates, under messy/multilingual input, 2026-07-11

`experiments/absorber/*` (no LLM). Tests whether a streaming statistical absorber (parallel work; the real `grounded_prose_absorber_incremental.py` was ABSENT from the repo — a minimal stand-in matching the described interface was built to keep the test real). Two honest, partly-negative results:

- **Absorber as a code-synthesis search prior — COMPLEMENTARY-BUT-WEAK** (`run_absorber_code_experiment.py`, real repo Python, held-out, streaming). Top-1 next-token accuracy **×2.1 vs uniform** (real local structure learned; operators discovered as pivots, PMI idioms mined, no English rules) — but **mean search rank ≈ uniform (0.97×)**: a token-bigram prior does NOT shrink the synthesis candidate set (too shallow for hierarchical/long-range code), and streaming improvement is marginal at this scale. It proposes candidates; it does not verify, and it does not meaningfully accelerate search.
- **The candidate_bridge integrity gate — PASS under MESSY/MULTILINGUAL/LOW-RESOURCE input** (`candidate_bridge.py` + `run_bridge_messy.py`). The binding rule holds on a hostile corpus (en/fr/es/pcm + JS + garbage, typos, inconsistent formatting): statistics only PROPOSE; execution DISPOSES. A **buggy `divide` with the HIGHEST discovery confidence (0.49) and most support (4) was QUARANTINED** (fails the b=0 test); the correct guarded `divide` (0.42) and a **noisy low-resource pcm `clamp` (conf 0.12, support 1) were VERIFIED and promoted** — verification is the equaliser that makes low-resource verified patterns first-class. Garbage/JS docs were skipped without crashing (messy-safe). **Only execution-verified units reach the ledger — never statistical strength.**
- **AST-structural absorber — tested, HONEST NEGATIVE on search reduction** (`ast_absorber.py` + `run_absorber_ast_experiment.py`). We hypothesised statement-type AST context would shrink synthesis search where token bigrams did not. Measured on held-out repo Python, with a FAIR interpolated-backoff rank (the strict-concatenation rank was an artefact that unfairly penalised sparse context): AST context lifts **top-1 accuracy 22.8%→30.8%** (real — it sharpens predictable boilerplate) but the **mean search rank is 0.98× uniform (no gain over bigram)** — it MISSES the agreed <0.5× bar. Graceful token fallback confirmed (2/40 unparseable docs fell back without crashing). The bridge still verified 2 candidates. **Why it plateaus (the deeper finding):** local statistical structure predicts *boilerplate* (brackets, common continuations) but NOT the information-bearing tokens — identifiers, values, the specific operation — that actually distinguish one program from another. Those come from the SPEC, not from surrounding code, so no statistical absorber (token or AST) will predict them. This reinforces that **verification is the highest-leverage area**, and that prose/code absorption should stay a *continuous statistical learning* layer, not be pushed to become a synthesis engine.
- **Honest conclusion:** absorption is a real, messy-robust, multilingual **candidate-discovery front-end** and a modest top-1 prior, NOT a solution to the verified-pattern-library blocker and NOT a synthesis-search reducer (measured, both token- and AST-level). It moves the DISCOVERY half modestly; the VERIFICATION half (the actual blocker) is untouched and stays the gate. Remaining gaps unchanged: prose→spec, non-Python extractors, runnable parameterised units, and — the real ceiling — the information-bearing content of code is spec-determined, not statistically predictable.

### End-to-end integration + multi-level evidence-driven knowledge, 2026-07-11

- **END-TO-END pipeline** (`run_pipeline_e2e.py`): the four components compose with guarantees intact — PLAN+SYNTHESISE a verified function library → a developer edit BREAKS a leaf (whole-program tests fail) → REPAIR from failing tests (18 evals) → REALISE a faithful NL report of the ACTUAL call graph, round-trip verified. **Program verified (0 wrong) · regression repaired · report faithful (0 fabrication).** The plan→synthesise→verify→repair→realise loop runs whole, no LLM.

- **M-KNOWLEDGE** (`run_m_knowledge.py`) — the continual-learning loop's hardest question, answered: *can the system reliably decide what general lesson to learn from a failure?* Multi-level (syntax/idiom/algorithm/architecture/debugging/planning), multi-verifier (compiler 0.3 < unit-test 0.7 < integration 0.85 < multi-context 0.95) knowledge store, tested against a NAIVE learner that promotes every first-seen fix globally (the "always `reverse=True`" mistake):
  - **UNIVERSAL vs CONTEXT-DEPENDENT** distinction (the safe-division vs sort-order/discount problem): promote to GENERAL only when a fix is verified-consistent across DIVERSE contexts; otherwise keep CONTEXT-SCOPED and ABSTAIN on a novel context rather than guess.
  - **Steady state: 100% applied-accuracy, 0 cross-context errors, 0 overgeneralizations** vs naive **289 / 47**. Honest nuance: exactly **1 learning-phase error** across 4 seeds (a context-dependent trigger whose sampled contexts accidentally agreed is indistinguishable from universal until evidence varies) → **self-corrected to 0 via demotion-on-contradiction** (confidence goes DOWN as evidence changes, not only up — knowledge is revisable).
  - **Verifier strength → confidence:** a compiler-only pattern stays below the reuse threshold and is abstained — grounding the "compilation proves syntax/type/link, not correctness (binary_search compiles yet fails on duplicates)" caveat.
  - **Domain-independence:** the trigger/context/fix/verifier abstraction is not coding-specific; the same loop covers medicine (guideline+outcome), law (jurisdiction), engineering (simulation), science (replicated studies), finance (backtest) — anywhere feedback is grounded. Where there is NO objective verifier (humor, beauty), it does not apply — named, consistent with the frontier boundary above.
  - **The answer:** yes, the system can reliably learn the right-level lesson — via evidence-bounded promotion + demotion-on-contradiction — with a bounded, self-correcting error, NOT a naive global patch. This is the general evidence-driven adaptation algorithm the analysis identifies as the architecture's core, now measured.

### M-ABSTRACT — learning the RIGHT abstraction from evidence, 2026-07-10

`experiments/synthesis/run_m_abstract.py` (offline, seeded, no LLM). Answers the analysis's hardest point — "extract the right level of abstraction from a failure without overgeneralising or under-reusing" (the sort()/discount trap). Hidden rule: `output = T(x) if C*(x) else x`; observe labelled examples; compare abstraction strategies on HELD-OUT (unseen, large input space):

- **evidence-bounded 100%** held-out accuracy — DISCOVER the most general predicate CONSISTENT with the evidence (ELE frequency+diversity promotion), apply the transform only under it.
- **over-general 46%** (always apply → wrong half the time) and **over-specific 54%** (memorise → no transfer to unseen). The right abstraction is neither.
- **conditions exactly recovered 5/6; accuracy 6/6** — in the one non-exact case the system learned a *different-but-equivalent* predicate that separates the same evidence (100% accurate). That is correct evidence-bounded behaviour — learn what the data supports, not a presumed rule — not a miss.
- **Conclusion:** the hard part of the continual-learning loop — *what general lesson to learn* — is solvable by bounding the abstraction to the evidence: general enough to reuse, specific enough not to regress, conditioned on discovered context. With M-LEARN (verified reuse) and M-PLAN (decomposition), this completes the grounded triad under the evidence-driven learning loop the analysis calls the architecture's strongest aspect.

### Scientific validation of trigger-discovery (CV + baselines + ablation) — HONEST, partly negative, 2026-07-11

`experiments/ele/{fetch_relation_facts,validate_relations}.py` (no LLM). A reviewer challenged: prove the method is genuinely general, not tuned to one benchmark — with **k-fold entity-disjoint cross-validation, statistical baselines, and ablation**, not one lucky split. Done, on REAL Wikidata facts + Wikipedia prose, **8 relations across geography/organisations** (P36 capital, P47 border, P112 founded-by, P159 HQ, P17 country, P38 currency, P37 language, P30 continent). First, two **data-soundness fixes** without which the stats measure artefacts: exact enwiki-title resolution (article coverage 77%→**100%**) and **complete KBs over the entity set** (a 100-pair border cap had truncated the KB, turning true border sentences into false negatives and destroying the trigger's measured precision — distant-supervision's classic incomplete-KB failure). Then, the honest findings — several **overturn the earlier optimistic single-split numbers**:

- **The method does NOT beat simple baselines.** 5-fold entity-disjoint, micro-averaged F1: contrast(ours) **25.6%**, frequency-only **26.4%**, TF-IDF **26.5%**, PMI 16.3%, most-frequent 11.0%, random 4.2%. Our positive/negative **contrast is statistically indistinguishable from a plain frequency-of-content-token baseline** (−0.9%, 95% CIs ±~20% overlap heavily). The earlier single-split "100% precision" was not robust; it was an underpowered, lucky split. This is the corrected, honest record.
- **Ablation names the component that actually matters: DIRECTION inference (−7.7% F1 when removed).** By contrast, removing the negative-contrast (+0.8%) or the precision threshold (+0.9%) slightly *helps* — the negatives add fragility under incomplete KBs, not value. And removing the **stopword filter *improves* F1 by +10.1%** (the content-token filter was over-restrictive; the precision threshold already guards function words). So the ablation says: keep direction inference, drop the stopword filter — an evidence-driven simplification, pending confirmation on more data.
- **Absolute F1 is low (~26%) with wide CIs** — extraction from tiny article *intros* with a single trigger is weak and underpowered (few entity pairs co-occur per fold). The bottleneck is **recall**, which motivates larger corpora (Phase 4) and multi-surface triggers / the adaptive lexicon (Phase 7). NOT claimed as strong; measured and named.
- **Anti-hardcoding still holds and is now stronger:** the file contains no relation name or trigger; the SAME code runs on all 8 relations. Generality of the *mechanism* is confirmed even though its *accuracy* is modest. Honesty over spin: rigorous validation reduced the numbers and corrected the story — which is the point.

**Study extended — Phases 4/5/7/11/12, 2026-07-11** (`adaptive_lexicon.py`, `open_discovery_eval.py`, `error_analysis.py`, `fetch_corpus_full.py`, `run_relation_study.py`; one-command `run_relation_study.py [--full]`). Same code, three corpus sizes (intro 247k words → full-article **2.08M words**). More honest, partly-negative findings:

- **Phase 11 error taxonomy explains the low recall and justifies the levers:** on intros the recall gap is **46% co-occur-but-no-known-trigger** (paraphrase → lexicon) + **44% entities-never-co-occur** (→ more corpus) + 1% coreference + 0.3% direction. ~90% of the gap is exactly Phases 7 and 4.
- **Phase 7 adaptive lexicon — the recall lever works, and the synonym vision is real.** Multi-surface triggers (top-1→top-5) lift recall on intros **10.9%→21.3%**. The discovered per-relation vocabulary is genuine and grows with data: on the 2M-word corpus P47 elects `bordered · bordering · neighboring · neighbouring` (both spellings) and P112 `founded · founder` — evidence-driven synonyms, no hand list. Stored with confidence·evidence (`results/adaptive_lexicon.json`).
- **Phase 4 — more corpus is a DOUBLE-EDGED sword (counter-intuitive, important).** 8× more text raised *extractable* pairs (more co-occurrence) but LOWERED extraction F1 (contrast 26.8%→14.8%; recall@5 21.3%→9.6%) because it also multiplied *incidental* co-occurrences (two entities in a sentence NOT expressing the relation) that a single-sentence trigger extractor misreads. Corpus size alone does not fix recall without relation-expression detection.
- **Phase 6 ablation, now decisive across 3 corpus sizes:** removing the stopword/content filter improves F1 by **+10% (intro) → +24% (2M)** — the biggest, most consistent effect; the content pre-filter is definitively over-restrictive. Negatives and the precision threshold also increasingly hurt at scale (+7% each on 2M). Direction inference helps on small corpora, neutral on large. **Evidence-based recommendation: drop the content filter and the negative-contrast; keep support-ranked triggers + direction — i.e., the simpler method the baselines already embody.**
- **Phase 5 open-discovery (Purity/NMI/ARI vs KB):** intro Purity 0.71 / NMI 0.40 / ARI 0.10 vs random 0.50 / 0.14 / 0.00 — recovers structure above chance but over-fragments (50 clusters for 8 relations); more text raises noise (cluster purity 92%→65%). Honest: emergent synonymy is real but noisy.
- **Bottom line (honest):** the *mechanism* is general (no relation/trigger coded; 8 relations, 2 domains, one command) and the *adaptive lexicon* genuinely learns synonym vocabularies from evidence — the architecturally important result. But single-sentence trigger extraction is **weak (F1 ~15–39%) and not improved by scale**; the honest next lever is detecting whether a co-occurrence *expresses* the relation (not just contains a trigger), which is the real open problem the numbers expose. Nothing inflated; the study is reproducible end-to-end.

### ELE relation extraction on a REAL corpus — self-supervised, held-out, multilingual, 2026-07-10

`experiments/ele/{fetch_wiki_corpus,run_relation_real_corpus}.py` (offline after fetch, no LLM). `run_relation_constructions.py` proved the construction-discovery *mechanism* but on 5 clean templates (the ideal world §0b warns against). This re-runs it on **real Wikipedia prose** with **self-supervised (distant-supervision)** labels and a **held-out-by-entity** split, in **two languages** — the messy-world test. Corpus: 92 EN + 75 FR real article intros (CC-BY-SA); seed KB = true `capital_of(capital, country)` facts; a sentence mentioning both members of a KB pair = a positive, two KB entities that are not a pair = a negative. The relation's trigger is DISCOVERED from the positive/negative contrast (highest-support high-precision between-entities token); direction learned by majority from the KB.

- **The trigger is learned per language, no hardcoded marker:** English data elects **`capital`**, French elects **`capitale`** — same mechanism, different language, zero markers coded. This is the multilingual anti-hardcoding thesis made concrete on real text.
- **Recall lifted from 0 → 71% (en) / 43% (fr), precision 100% both**, held out by entity. The live high-precision baseline (`grammar_relations`) scores **0% recall** here because it ABSTAINS on real phrasing ("is the capital of" carries ≥2 function words → its precision guard fires) — safe but useless for recall. The discovered trigger recovers the relation at **100% precision, 0 wrong-direction, 0 fabricated edges**.
- **Honest imperfections, kept in view:** (1) recall is real-world, not a suspicious 100% — real prose writes "Ottawa is Canada's capital" (trigger outside the between-window) or double-mentions the entity, which a single-sentence extractor misses; recall is over the *extractable* (co-occurring-within-window) test pairs. (2) French needed a larger fetched slice than English before `capitale` cleared support — a genuine **data-sparsity / breadth** datapoint (the [[jimsai-multilingual-bottleneck]] theme), fixed by ingesting *more real data*, NOT by lowering the threshold (which would admit distant-supervision artefacts like the spurious French `économique` that a small negative set let through). (3) precision is closed-world over KB entities (direction + no-false-edge is real; open-world NER precision is separate, untested here).
- **Promotion consequence:** this is the exact metric the **traversal reasoner** was gated on (§ live-promotion: "recall grows with ELE discovery"). A learned, self-supervised, multilingual path now lifts relation-extraction recall well above the abstain-only floor **at 100% precision** — the never-voice-a-wrong-edge property the reasoner requires is preserved while recall becomes useful. Remaining: more relations (not just `capital_of`), open-world entities, cross-sentence pairs.

### M-PLAN — planning by decomposition scales verified synthesis, 2026-07-10

`experiments/synthesis/run_m_plan.py` (offline, seeded, no LLM). The move needed for large code / massive generation: decompose a request into INDEPENDENTLY VERIFIABLE sub-tasks, solve each with the verified loop, compose. Tested on multi-output tasks (a function returning a tuple — each field a natural sub-goal), at the SAME node budget:

- **decomposition solves 60/60 (100%) vs monolithic 14/60 (23%)** — reach lift +46. The monolithic search space is the PRODUCT of the per-field spaces (combinatorial); decomposition makes it a SUM. That is why decomposition scales where flat search does not.
- **0 wrong** — each field program is executed against its examples and the composition against the whole-tuple examples before acceptance; a wrong sub-solution fails verification and is rejected. The never-voice-a-wrong-program guarantee is preserved UNDER COMPOSITION.
- **Honest boundary:** this proves that GIVEN a decomposition, verified synthesis scales. Automatically DISCOVERING good decompositions for arbitrary prose specs is the harder open problem (the planning analogue of ELE construction discovery) — named, not faked. Multi-output structure is one real, common decomposition. Together with M8 (synthesis) and M-LEARN (verified-pattern reuse), this is the grounded spine of scaling to large verifiable generation: decompose → synthesise-and-verify each part → reuse learned patterns → compose, with correctness guaranteed at every join.

### M8 — Verification-first code synthesis: proof of mechanism, 2026-07-08

`experiments/synthesis/run_m8.py` (offline, seeded, no LLM). Answers the hardest "is it possible without an LLM?" question empirically. Bounded typed search over an operation grammar + execution-verification (CEGIS-shape); generated tasks (random params per seed), success = generalisation to HELD-OUT examples the program was not fit on.

- **In-grammar: 160/160 (100%) verified-solve, 0% overfit** across 4 seeds — small integer/list functions ARE synthesisable with no model, and they generalise.
- **Boundary tier (tasks needing constructs outside the grammar — filters, conditionals, x³): 80/80 (100%) REFUSED, 0 wrongly-voiced.** The mechanism finds no program rather than fabricating one. **This is the Independence Policy proven for code: it may fail to solve, but never presents a program that passes training yet fails holdout.**
- **Honest conclusion:** verification-first code synthesis is real and safe at toy scale; growing the grammar/search budget moves the boundary without compromising the never-voice-a-wrong-program guarantee. Frontier scale (architecture, multi-module, learned SE templates) is M8-scale, still roadmap — but the *mechanism* is grounded, not theory.

### M5 (breadth) + cross-lingual recall — the two P4-Yoruba failures closed, 2026-07-09

The prior board's remaining 2 P4 failures were attributed to "lexicon data — yo 1.8k entries" (line 165). Buying that data exposed the *actual* bottleneck, which was not coverage. Three grounded steps, each measured:

- **Breadth ingestion (`experiments/concept_model/broaden_lexicon.py`, QRank [3000,15000)):** lexicon 39k→**155k** entries (`city`→`cité`/`城市`, `engineer`→`ingénieur`/`工程師` now resolve). This FIXED the *realization* half of P11 — content nouns translate — but did **not** fix P4-Yoruba recall. Finding: coverage was not the recall bottleneck.
- **Language detection was a hidden blocker (`surface_realizer.detect_language`).** It called a `vote_language` method that never existed, so detection used script/diacritic only; diacritic-free French ("Dans quelle ville…") misdetected as English and the answer stayed English. Fixed with an **inverse-language-frequency vote over lexicon surface membership** (a token exclusive to one language, e.g. `ville`→fr, votes strongly; one shared across many, e.g. `se`→5 langs, barely), plus a **content-word length gate (≥4)** so short code/abbrev surfaces ("est","la","du" as English) can't drown the signal. No word list — the evidence is the Wikidata lexicon. Detection went 9/12→**12/12** on a fr/en/zh/yo probe; fr and zh now realize end-to-end.
- **Cross-lingual recall — the real P4-Yoruba root cause (`runtime_layers._memory_excerpt_steps`).** Trace showed the Yoruba fact WAS retrieved (`L6 count=1`) but the reasoning bridge never voiced it: the claim-selection min-score was 2, Yoruba shares ~1 surface term with an English-stored fact ("Ìlú"≠"city"), and the concept boost was **0** because the broadened lexicon let *"the city"* greedily match a NAMED entity (C:Q23311, "the City"/London) instead of the city concept (C:Q515) — so the concept the query resolved never appeared in the fact's encoding. Fix: a sentence carrying the **exact queried entity (a language-neutral nonce literal) is strongly anchored** and needs only min-score 1; the entity match is itself the evidence. Anti-leak precision is unaffected — a neighbor fact lacking the entity was already dropped by the scope gate.

**Measured (seed 44241, fully de-LLM/local, fr/yo/zh): 11/11 properties at 100%** — up from 10/11 (P4 78%). P4_multilingual 7/9→**9/9** (both Yoruba failures closed). P11_language 3/3. Critically the anti-leak trio held: **P2 gap-honesty 3/3, P7 scoping 2/2, P9 edge 11/11** — the entity-anchor threshold did not open a leak. Cross-seed generalization CONFIRMED across three independent seeds — **P4_multilingual 9/9 on all three (44241, 7788, 55129)**, P11 3/3 on all three, and the anti-leak trio (P2/P7/P9) 100% on all three (the entity-anchor threshold opened no leak on any seed). Full sweeps: 44241 **11/11**, 7788 **11/11**, 55129 **10/11** — the sole blemish is P8_dialogue 3/4 (a dialogue-followup referent case, non-deterministic — earlier traces show a person_city followup occasionally routing to world_knowledge), which is outside this work's scope (detection/voicing/multilingual) and is recorded here as-is rather than patched. The multilingual fix is robust; the P8 flake is a separate, named open item.
- **Residual honest limitation:** a Yoruba sentence with tone marks but *no* sub-dot letters (`Ìlú wo ni … wà?`) still misdetects as French for *realization* (à/ù overlap the French accent repertoire; only sub-dots are Yoruba-exclusive orthography). Recall is correct (entity-anchored, language-neutral); only the answer's surface language is wrong on that adversarial string. Real Yoruba text carries sub-dots and detects correctly (yo-case-2 realizes `line`→`gbólóhùn`). WSD-class polysemy also survives: `lead engineer`→`plomb`/`铅` (the metal). Both are named ceilings, not hidden.

### M12 — Physical Reality Engine: the verify-before-realize spine transfers to media, 2026-07-09

`experiments/media/run_m12_reality.py` (offline, seeded, no LLM, no GPU, no model). Answers the direct question behind the "Media Realization Engine" direction: does the JimsAI spine — evidence → structure → projection → **verify** → realize — transfer to a NON-language modality, or is it a language-only trick? M8 proved the spine for code; this tests it on 2D physical scenes (light, objects, shadows, occlusion). The verifier's job is the one the vision names: not to render but to **ensure reality** — "is physically consistent," not "looks right."

The verifier is handed a hypothesis space of candidate relations (the analog of M8's operation grammar) but **never the physics**. Three falsifiable claims, all measured across 4 seeds:

- **C1 — invariant discovery from evidence (not hardcoded).** From consistent scenes alone it discovered all four real laws every seed — shadow direction = f(light azimuth), shadow length = k·height/tan(elevation) (it picked the right basis among decoys, proven by held-out generalization across elevations), objects rest on the ground, nearer occludes farther — by keeping a candidate iff it holds with ~zero residual across ALL evidence. Critically it **rejected the spurious decoy relation every seed** (a random feature dressed as a law): discovery certifies invariants and refuses non-invariants, the false-positive guard that makes "discovered, not hardcoded" a real claim.
- **C2 — the refusal guarantee (the M8 contract, for media).** On held-out scenes: **100% accept** on novel consistent ones (composition/projection it never trained on), **100% refuse** on physically-impossible ones with the violated law **named** (100%), and — the guarantee — **0 impossible scenes accepted** across all seeds. It may fail to realize, but never realizes an impossibility.
- **C3 — one concept, many realizations.** The SAME verified scene structure realizes deterministically as an SVG (visual, `results/m12_scene.svg`) AND as a sentence ("A scene lit from the SW at golden hour (14° elevation); 3 objects rest on the ground, casting shadows toward NE.") — no model in either path. SW light → NE shadow (180° opposite), 14° elevation → warm/golden: physically coherent because it was verified before it was realized.

**Honest scope.** This is proof of MECHANISM at toy scale, exactly like M8 — it does NOT claim frontier image quality, pixel synthesis, or physics learned from photographs. The hypothesis space is hand-specified (as M8's grammar is), and growing it to real perceptual evidence (learned visual constructions from image corpora, richer physics) is the frontier, still roadmap. What is grounded, not theory: the **verify-before-realize spine and its refusal guarantee are modality-general** — the same contract that keeps text from voicing an unproven fact and code from voicing a wrong program keeps media from realizing an impossible scene. The vision's "Physical Reality Engine = verification discovered from evidence" is buildable and behaves as claimed on the falsifiable slice. See `docs/generation_decomposition.md` §Media for the full assessment of which parts of the multimodal vision map to built proof points vs. remain research bets.

### P8 dialogue — two independent referent-resolution bugs, root-caused and fixed, 2026-07-09

The P8 blemish flagged above (seed 55129, 3/4) was diagnosed the same disciplined way — trace, don't guess — and turned out to be **three independent bugs** across different families, not one flake. Each fix is a general mechanism with zero test-specific values:

- **person_city — routing (`capability_router.py`).** The pronoun follow-up "And which city is she based in?" *did* resolve "she"→the workspace entity and recall the fact, but routed to **world_knowledge** (5/5 trials): its surface embeds like a geography question (score 0.486) and beat memory_chat (0.395) even after the workspace-literal boost fired. Result: correct answer voiced but polluted by external junk ("Cardi B – Wikipedia") that intermittently displaced it → the non-deterministic P8 fail. **Fix (principled):** a known workspace entity as the query's subject — literal *or* discourse-resolved pronoun — means the subject is PRIVATE; WORLD_KNOWLEDGE (about PUBLIC entities) cannot be the *primary* intent, so it is capped just below memory. This completes the standing "a query naming a known workspace entity is a memory question regardless of wording" rule, which a mere score *boost* did not enforce. It only triggers when world_knowledge would otherwise win (wk ≥ mem), so multi-intent (P5, math≈world_knowledge=0) is untouched.
- **device_codename — discourse focus (`pipeline.py`).** The opener "What is the codename of the device replacing the Zuxfinve line?" set the thread's ACTIVE_OBJECT to **"what"** — L1 over-extracts the wh-word into its entity list, and focus took `entities[0]`. So the follow-up's "it" resolved to "what" and recalled nothing. **Fix (principled):** a discourse referent is a NAMED entity, and the CLL's name-evidence literals are exactly that (a nonce encodes as `L:…`; "what" does not). Focus now prefers the name-evidence literal, falling back to L1 entities only when the query names nothing — one name-evidence machinery, any language, **no wh-word stoplist**.
- **query-echo — content hygiene (`retrieval.py`), the actual seed-55129 harness failure.** The single-fact reproductions of the two bugs above passed, but the harness accumulates prior turns (P1/P3/P4/P5 run before P8 in the same workspace), and the device follow-up voiced **stored prior queries** — "what is THE codename OF the device REPLACING the Zuxfinve line", the typo'd "waht is the codenmae…" — instead of the fact. Two things combined: the casing/punctuation perturbation **strips the "?"**, so the interrogative-filter (trailing-"?") no longer catches it; and the input-query signature (`provenance="local_extraction"`) is skipped by the main retrieval loop but the **concept-index injection did not replicate that skip**, so it re-surfaced the stored question (whose concepts match the current one). **Fix (principled):** the concept-index injection now skips `local_extraction` records exactly as the main loop does — a "voice only knowledge, never interaction logs" invariant applied consistently across both retrieval paths. Taught facts never carry that provenance, so recall is unaffected (verified: device now voices only "The device codenamed Wexplor replaces the Zuxfinve line."). This is broader than P8 — it closes a query-echo hazard on any workspace with prior turns.

**Direct verification (post-fix):** all three follow-up families recall 2/2 (`project_database`, `person_city`, `device_codename`), the harness-shaped device case (accumulated perturbed prior queries) now recalls the fact with no query-echo, and the cold-thread control correctly refuses (no prior turn ⇒ no focus ⇒ no leak). All three fixes contain zero test-specific values. Harness re-measure (55129 + fresh seed) confirming P8 4/4 with no regression: pending, recorded as-is when it lands.

### M2 — T1-mini v2 (the real T1 replacement)
Export SPPE pairs as (concept sequence → intent); train the tiny classifier; benchmark against Qwen3-1.7B **on the same routed live queries**, including the v11 chaos list (typos, code-switching, pasted logs, multi-intent).
- **Pass:** ≥ T1 parity accuracy at <1% latency; ships behind a flag with confidence-gated LLM fallback; fallback rate reported on a dashboard, expected to fall over time.
- **Kill:** if concept-native classification plateaus materially below Qwen accuracy on live chaos (not toy prompts), T1 stays; T1-mini becomes a pre-filter that skips T1 only above a calibrated confidence — still a cost win, honestly scoped.

### M3 — ELE discovery on real text (the "verb = 2nd token" reckoning, spec §7) — ✅ DONE 2026-07-07
Stress the discovery pipeline against fronted adjuncts, passives, subordinate clauses, questions — sampled from real corpora (Rule 2 style), not constructed sentences.
- **Pass:** discovery works from surface distribution alone, or the spec records exactly which structural signals had been silently supplied by the toy heuristic and what replaces them (this is a *finding*, either way).
- **Kill:** if construction discovery fundamentally requires parser-grade structure on real text, ELE's parse side is re-scoped: frames come from L1's existing extraction + discovered constructions as a *supplement*, and the "parser-free" claim is retired.

**Result (2026-07-07, `experiments/ele/m3_real_text.py`, real prose sampled at runtime from this repo's documentation; JSON in `experiments/ele/results/m3_real_text.json`):**
- **M3a PASS — discovery itself is parser-free on real text:** 518 constructions promoted from 418 real sentences; the top frames' fixed tokens sit 100% in the corpus's top-decile frequency stratum — function-word scaffolding (`the %`, `% is`, `of %`, …) identified distributionally, with no stopword list anywhere.
- **M3b FINDING — the classifier's *inputs* do not come from position heuristics.** Two artifacts were found and controlled for: (1) any predicate rule that picks tokens from the sentence saturates obligatoriness to 1.0 on self-matching tails (confound guard added); (2) random predicate assignment fragments evidence below the sample gate (controls: 60/60 UNPROVEN), so agreement comparisons cannot certify the heuristic. Decisively: the tails discovery surfaces on real text are determiner/copula scaffolding — not argument-structure units — so classifying them as CORE/ADJUNCT is a category error regardless of predicate rule.
- **Conclusion (the pre-declared re-scope applies):** the toy corpora silently supplied *two* structural gifts — predicate identity and meaningful candidate argument units. On real text both must come from L1's structured extraction or a bounded parser interface (perception-grade trained component, knowledge-free, per CLL hard truth 12). Obligatoriness/spread classification remains valid *once those inputs exist* (proven in the seeded suite, R11–R13b). The "parser-free discovery" claim survives; the "parser-free argument-structure classification" claim is retired, in writing, here.

### M4b — constrained-decode realizer (the T2 endgame mechanism) — ✅ MECHANISM PROVEN 2026-07-07
`experiments/ele/realizer.py`, seeds 702945 / 4242 / 1 / 999999, JSON in `experiments/ele/results/m4b_realizer.json`. A ~22k-param in-house neural realizer decodes over a content-licensed lattice and must LEARN agreement (article form = f(noun class, number)) and plural morphology — the fluency decisions templates would need precomputed upstream.

- **F1 — zero invention is a property of the decode space:** at an undertrained checkpoint, the *same weights* hallucinate unlicensed content on 18–23/24 held-out frames unconstrained, and on **0/24 constrained** — across all seeds and both languages. Training quality cannot break the guarantee.
- **F2 — fluency beyond templates:** 100% held-out agreement and 100% exact match (all seeds, both languages) on compositional (agent, object) × number frames reserved before generation.
- **F3 — new language = data only:** a second grammar (fresh function words, SOV order — word order is grammar *data*) met F1+F2 with the identical class; no language branch exists in the file.
- **Finding (earned through two real failures):** closed-class vocabulary must be defined *frequency-free*: a token is closed-class iff it never appears as a frame value AND is not a (value + mined suffix) variant. Two frequency-based definitions failed first — unbalanced noun classes pushed rare plural articles below any frequency bar, masking the correct form out of the lattice (agreement 0.50, then 0.75, then 1.00 after the fix). The failure history is preserved in the module docstring.
- **Scope honesty:** proven at nonce-grammar scale with two fluency phenomena. The production path (real SPPE pairs, real morphology across languages, VCO-scale content licensing) is M4b-production, judged by content-fidelity property tests + fluency comparison against approved human-rated responses.

### Directive update (2026-07-07): full LLM independence
Per project direction, the architecture drops LLM fallback entirely — even on hard cases. See `docs/jimsai_llm_free_architecture.md` §2.6 (Independence Policy): outside coverage the system CLARIFIES (bounded question rendered from the ambiguity set), REFUSES with the gap named (typed GapReport), or GROWS (gap → coverage backlog). External models survive only as frozen measurement baselines in the harness (M6). The measurable bet: clarify/refuse rates fall over time while task success rises — tracked on the dashboard, falsifiable in public.

### M4 — ELE render as CSSE expansion (T2 deterministic share)
Wire the shape+adjunct renderer into the CSSE path with `GapReport` → Qwen3-4B fallback (flagged unverified). Templates derived from M3 discovery output per E-Rule 1.
- **Pass:** deterministic-render share measurably rises on live traffic; **zero hallucinations within the deterministic share** on generated held-out compositional combos (the row-6 property, at production scale, fresh seeds); fluency judged acceptable on the factual/structured slice.
- **Kill:** if deterministic share stalls at a trivial fraction (<~10% of renderable responses) after coverage growth, the renderer remains a CSSE feature and the "T2 replacement" framing is dropped from the spec.

### M5 — Knowledge breadth (CLL Gap 1; makes the system *feel* capable)
Bulk-ingest Wikidata/ConceptNet slices as provenance-stamped graph substance; property test = mask held-out edges, ask generated questions (never enumerated).
- **Pass:** answerable-fraction grows monotonically per million edges ingested; refusal stays calibrated (reversed-edge honesty stays ≥ the v1.1 bar).
- **Kill:** if answerable-fraction saturates early, the bottleneck (inference depth vs coverage vs disambiguation) is identified by ablation before more ingestion is bought.

### Continuous (never "done")
- Equal-budget neural baseline regression (E-Rule 4) on every ELE mechanism claim.
- Weekly coverage dashboards (CLL Rule 4): % tokens → concept per language, deterministic-render share, T1-mini fallback rate, refusal calibration.
- LLM-baseline win-rate on the generative harness: same generated inputs to JimsAI and a frozen frontier-model baseline; report accuracy × provenance × calibrated-refusal per property. This is the only legitimate scoreboard for the "quality as good as frontier" ambition — per-distribution, measured, and it will show losses honestly (expect to lose Gaps 5–7 indefinitely; expect to win P4-style properties immediately).

---

## 6. Summary judgment

The architecture's honesty infrastructure is genuinely unusual and is the thing to protect: labeled evidence, committed negative results, anti-hardcoding rules with no legal path around them, and a generative harness as the only judge. The CLL side is real, committed, and one flag-flip away from its first production win (M1). The ELE side is currently *a well-written memory of experiments that no longer exist* — M0 is not bureaucracy, it is the difference between an engineering program and a story. Frontier-quality is reachable only as a per-distribution, measured claim — coverage growth plus honest fallback — and the roadmap above is the shortest falsifiable path to it.

---

## 7. Production activation + lexicon-as-cloud-artifact (2026-07-11)

Two deployment defects made the validated M1 win **dormant in production**, and fixing them the scalable way changed where the lexicon lives.

- **The concept index was `off` in prod.** `JIMS_CONCEPT_INDEX` defaults to `"off"` (`cll_shadow.index_enabled`), the deploy manifest never set it, and `.env` didn't carry it — so every M1/M2 cross-lingual mechanism (P4 0%→67%) was shipped-but-switched-off. The Lambda answered `deterministic:true` the whole time, masking it. Fixed: `JIMS_CONCEPT_INDEX=on` in both the deploy manifest and `.env`; the winning de-LLM config (concept index on, no reachable LLM → CSSE renders, honest gaps) is now the deployed config, not just the doc's "M6 to-do".
- **The lexicon was about to be baked into the deployment package** — the "no local growing list" anti-pattern for a *read* artifact. A Lambda filesystem is read-only, training/enrichment cannot rewrite a bundled file, and a code-frozen copy drifts from local dev. Resolution: the lexicon, learned common-words, and concept edges live once in **R2** (`concept-model/*.jsonl`), the single source of truth for local AND deploy. `cloud_artifact.py` fetches them per cold start into `/tmp` with ETag revalidation and a graceful local fallback (the object-store analogue of `cloud_counter.CloudCounter`); `cll_shadow` and `surface_realizer` load through it; `seed_lexicon.py` seeds/refreshes. **Training closes the loop:** every enrichment writer (`build/broaden/enrich_lexicon`, `fetch_common_words`) auto-publishes its grown artifact to R2, so a training run updates the shared lexicon and the service picks it up on next cold start — no redeploy, no bundled data. This is the general form of the rule that already governed `_bg_df` (runtime counters → Redis): **growing data belongs in a provider, never in the release.**
- **Honest residuals.** Yoruba recall is still coverage-limited (`yo` lexicon is thin — the M5 grind), but the fix path is now data-only: enrich → auto-publish to R2 → live, with no code change or redeploy. Event sourcing still writes to ephemeral `/tmp` on Lambda, and several pipeline collections remain in-memory — the row-store migration (Supabase/Neo4j) is the next durability chunk, tracked separately.
